apply plugin: 'maven-publish'

publishing {
    publications {
        upush(MavenPublication) {
            groupId 'com.umeng.message.lib'
            artifactId 'PushSDK'
            version '3.1.1'

            artifact 'PushSDK/build/outputs/aar/PushSDK-release.aar'
        }
    }
}

